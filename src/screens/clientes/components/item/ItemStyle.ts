import styled from "styled-components/native"
import { ListItem, ListItemSubtitle, ListItemTitle } from "../../../../components/form"

export const Container = styled(ListItem)``

export const Content = styled.View``

export const Title = styled(ListItemTitle)``

export const Subtitle = styled(ListItemSubtitle)``