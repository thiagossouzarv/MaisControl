import styled from "styled-components/native"
import { ListItem, ListItemTitle } from "../../../../components/form"

export const Container = styled(ListItem)``

export const Label = styled(ListItemTitle)``